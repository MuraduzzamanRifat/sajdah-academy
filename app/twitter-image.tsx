/* Twitter card uses the same image as Open Graph for visual consistency.
   Re-exporting from opengraph-image keeps a single source of truth. */
export { default, alt, size, contentType, dynamic } from "./opengraph-image";
